---
apiVersion: batch/v1
kind: Job
metadata:
  name: restore-lidarr
  namespace: media
spec:
  template:
    spec:
      containers:
        - name: restore-lidarr
          image: ghcr.io/ahinko/simple-k8s-backup:v3.0.4
          args:
            - /run.sh
          env:
            - name: FORCE_RESTORE
              value: "1"
            - name: MINIO_ACCESS_KEY
              value: "${SECRET_MINIO_ACCESS_KEY}"
            - name: MINIO_SECRET_KEY
              value: "${SECRET_MINIO_SECRET_KEY}"
            - name: MINIO_HOST
              value: "https://s3.${SECRET_DOMAIN}"
            - name: MINIO_BUCKET
              value: "backups"
            - name: BACKUP_NAME
              value: "lidarr"
          volumeMounts:
            - name: lidarr-storage
              mountPath: /lidarr
      volumes:
        - name: lidarr-storage
          persistentVolumeClaim:
            claimName: lidarr-config
      restartPolicy: Never
  backoffLimit: 4
