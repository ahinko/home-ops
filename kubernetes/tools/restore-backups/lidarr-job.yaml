---
apiVersion: batch/v1
kind: Job
metadata:
  name: restore-lidarr
  namespace: downloads
spec:
  template:
    spec:
      containers:
        - name: restore-lidarr
          image: ghcr.io/ahinko/simple-k8s-backup:4.2.61
          args:
            - /run.sh
          env:
            - name: FORCE_RESTORE
              value: "1"
            - name: MINIO_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: backups-secrets
                  key: MINIO_ACCESS_KEY
            - name: MINIO_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: backups-secrets
                  key: MINIO_SECRET_KEY
            - name: MINIO_HOST
              valueFrom:
                secretKeyRef:
                  name: backups-secrets
                  key: MINIO_HOST
            - name: MINIO_BUCKET
              value: "backups"
            - name: BACKUP_NAME
              value: "lidarr"
          volumeMounts:
            - name: lidarr-storage
              mountPath: /lidarr
      volumes:
        - name: lidarr-storage
          persistentVolumeClaim:
            claimName: lidarr-config
      restartPolicy: Never
  backoffLimit: 4
