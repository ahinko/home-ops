---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: backup-lidarr-config
spec:
  schedule: "0 9 * * *"
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 1
  jobTemplate:
    spec:
      template:
        spec:
          # Stagger jobs to run randomly within X seconds to avoid backing up all apps at once
          initContainers:
            - name: sleep
              image: ghcr.io/ahinko/simple-k8s-backup:v4.2.70
              command: ["/sleep.sh"]
              args: ["1", "300"]
          containers:
            - name: backup-lidarr
              image: ghcr.io/ahinko/simple-k8s-backup:v4.2.70
              args:
                - /run.sh
              envFrom:
                - secretRef:
                    name: backups-secrets
              env:
                - name: BACKUP_NAME
                  value: &appname lidarr
                - name: DELETE_OLDER_THAN
                  value: "14d"
                - name: EXCLUDE
                  value: "./logs logs* ./backups ./MediaCover"
                - name: HEALTHCHECK_URL
                  value: "http://uptime-kuma.monitoring:3001/api/push/v5FudMtfCi?status=up&msg=OK&ping="
              volumeMounts:
                - name: backup-persistent-storage
                  mountPath: /lidarr
          restartPolicy: Never
          volumes:
            - name: backup-persistent-storage
              persistentVolumeClaim:
                claimName: lidarr-config
          affinity:
            podAffinity:
              requiredDuringSchedulingIgnoredDuringExecution:
                - labelSelector:
                    matchExpressions:
                      - key: app.kubernetes.io/name
                        operator: In
                        values:
                          - *appname
                  topologyKey: kubernetes.io/hostname
