---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: backup-lidarr
  namespace: media
spec:
  schedule: "32 9 * * *"
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 1
  jobTemplate:
    spec:
      template:
        metadata:
          annotations:
            botkube.io/disable: "true"
        spec:
          containers:
            - name: backup-lidarr
              image: ghcr.io/ahinko/simple-k8s-backup:v3.0.9
              args:
                - /run.sh
              env:
                - name: TZ
                  value: "${TIMEZONE}"
                - name: MINIO_ACCESS_KEY
                  valueFrom:
                    secretKeyRef:
                      name: backup-secrets
                      key: SECRET_MINIO_ACCESS_KEY
                - name: MINIO_SECRET_KEY
                  valueFrom:
                    secretKeyRef:
                      name: backup-secrets
                      key: SECRET_MINIO_SECRET_KEY
                - name: MINIO_HOST
                  valueFrom:
                    secretKeyRef:
                      name: backup-secrets
                      key: SECRET_MINIO_DOMAIN
                - name: MINIO_BUCKET
                  value: "backups"
                - name: DELETE_OLDER_THAN
                  value: "14d"
                - name: BACKUP_NAME
                  value: "lidarr"
                - name: EXCLUDE
                  value: "./logs logs* ./backups ./MediaCover"
                - name: HEALTCHECK_URL
                  value: "https://uptime.${SECRET_DOMAIN}/api/push/v5FudMtfCi?status=up&msg=OK&ping="
              volumeMounts:
                - name: lidarr-persistent-storage
                  mountPath: /lidarr
          restartPolicy: Never
          volumes:
            - name: lidarr-persistent-storage
              persistentVolumeClaim:
                claimName: lidarr-config
