---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 40Ti
  storageClassName: ""
  volumeMode: Filesystem
  volumeName: storage-zfs-media
