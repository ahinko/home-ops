---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: backup-sonarr
  namespace: media
spec:
  schedule: "40 9 * * *"
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 1
  jobTemplate:
    spec:
      template:
        metadata:
          annotations:
            botkube.io/disable: "true"
        spec:
          containers:
            - name: backup-sonarr
              image: ghcr.io/ahinko/simple-k8s-backup:v4.0.0
              args:
                - /run.sh
              env:
                - name: TZ
                  value: "${TIMEZONE}"
                - name: MINIO_ACCESS_KEY
                  valueFrom:
                    secretKeyRef:
                      name: backup-secrets
                      key: SECRET_MINIO_ACCESS_KEY
                - name: MINIO_SECRET_KEY
                  valueFrom:
                    secretKeyRef:
                      name: backup-secrets
                      key: SECRET_MINIO_SECRET_KEY
                - name: MINIO_HOST
                  valueFrom:
                    secretKeyRef:
                      name: backup-secrets
                      key: SECRET_MINIO_DOMAIN
                - name: MINIO_BUCKET
                  value: "backups"
                - name: DELETE_OLDER_THAN
                  value: "14d"
                - name: BACKUP_NAME
                  value: "sonarr"
                - name: EXCLUDE
                  value: "./logs 'logs*'"
                - name: HEALTCHECK_URL
                  value: "https://uptime.${SECRET_DOMAIN}/api/push/QQaEVLv6IU?status=up&msg=OK&ping="
              volumeMounts:
                - name: sonarr-persistent-storage
                  mountPath: /sonarr
          restartPolicy: Never
          volumes:
            - name: sonarr-persistent-storage
              persistentVolumeClaim:
                claimName: sonarr-config
