---
apiVersion: v1
kind: Service
metadata:
  name: emqx
  labels:
    apps.emqx.io/db-role: core
    apps.emqx.io/instance: emqx
  annotations:
    metallb.universe.tf/loadBalancerIPs: "${LB_EMQX}"
spec:
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 1883
  selector:
    apps.emqx.io/db-role: core
    apps.emqx.io/instance: emqx
