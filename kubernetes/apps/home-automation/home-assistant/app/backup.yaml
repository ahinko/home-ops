---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: backup-home-assistant-config
  namespace: home-automation
spec:
  schedule: "0 9 * * *"
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 1
  jobTemplate:
    spec:
      template:
        spec:
          # Stagger jobs to run randomly within X seconds to avoid backing up all apps at once
          initContainers:
            - name: sleep
              image: ghcr.io/ahinko/simple-k8s-backup:v4.2.59
              command: ["/sleep.sh"]
              args: ["1", "300"]
          containers:
            - name: backup-home-assistant
              image: ghcr.io/ahinko/simple-k8s-backup:v4.2.59
              args:
                - /run.sh
              envFrom:
                - secretRef:
                    name: backups-secrets
              env:
                - name: BACKUP_NAME
                  value: &appname home-assistant
                - name: DELETE_OLDER_THAN
                  value: "14d"
                - name: EXCLUDE
                  value: "./home-assistant_v2.db ./.vscode ./core"
                - name: HEALTHCHECK_URL
                  value: "http://uptime-kuma.monitoring:3001/api/push/4rhMSzKu9d?status=up&msg=OK&ping="
              volumeMounts:
                - name: backup-persistent-storage
                  mountPath: /home-assistant
          restartPolicy: Never
          volumes:
            - name: backup-persistent-storage
              persistentVolumeClaim:
                claimName: home-assistant-config
          affinity:
            podAffinity:
              requiredDuringSchedulingIgnoredDuringExecution:
                - labelSelector:
                    matchExpressions:
                      - key: app.kubernetes.io/name
                        operator: In
                        values:
                          - *appname
                  topologyKey: kubernetes.io/hostname
