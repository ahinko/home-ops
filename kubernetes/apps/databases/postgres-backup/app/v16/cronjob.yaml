---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: postgres16-backup
spec:
  schedule: "0 4 * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 1
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: postgres-backup
              image: ghcr.io/ahinko/simple-pg-backup:v2.1.29
              imagePullPolicy: IfNotPresent
              args:
                - /run.sh
              env:
                - name: POSTGRES_DB
                  value: "nextcloud,miniflux,immich,homeassistant,mastodon,kitchenowl,atuin,gatus,element"
                - name: POSTGRES_HOST
                  value: "postgres16-ro.databases"
                - name: POSTGRES_EXTRA_OPTS
                  value: "-b -C -c -Z6 --inserts"
                - name: BACKUP_NAME
                  value: "postgres"
                - name: MINIO_BUCKET
                  value: "backups"
                - name: MINIO_HOST
                  value: "http://minio.storage:9000"
              envFrom:
                - secretRef:
                    name: postgres16-backup-secret
          restartPolicy: OnFailure
