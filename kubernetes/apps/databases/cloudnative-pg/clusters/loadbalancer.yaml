---
apiVersion: v1
kind: Service
metadata:
  name: postgres-loadbalancer
  namespace: databases
  labels:
    postgresql: postgres
    role: primary
spec:
  ports:
    - protocol: TCP
      port: 5432
  selector:
    postgresql: postgres
    role: primary
  type: LoadBalancer
  loadBalancerIP: ${LB_POSTGRES}
