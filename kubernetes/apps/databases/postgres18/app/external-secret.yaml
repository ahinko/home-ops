---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: postgres18-backup
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: postgres18-backup
  dataFrom:
    - extract:
        key: postgres18-backup
