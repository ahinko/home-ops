---
apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: lb-layer2
spec:
  ipAddressPools:
    - lb-pool
  nodeSelectors:
    - matchLabels:
        kubernetes.io/hostname: atlas
    - matchLabels:
        kubernetes.io/hostname: poseidon
    - matchLabels:
        kubernetes.io/hostname: eos
    - matchLabels:
        kubernetes.io/hostname: hades
