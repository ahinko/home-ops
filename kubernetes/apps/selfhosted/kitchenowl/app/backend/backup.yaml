---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: backup-kitchenowl
spec:
  schedule: "0 9 * * *"
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 1
  jobTemplate:
    spec:
      template:
        spec:
          # Stagger jobs to run randomly within X seconds to avoid backing up all apps at once
          initContainers:
            - name: sleep
              image: ghcr.io/ahinko/simple-k8s-backup:v4.2.62
              command: ["/sleep.sh"]
              args: ["1", "300"]
          containers:
            - name: backup-nextcloud
              image: ghcr.io/ahinko/simple-k8s-backup:v4.2.62
              args:
                - /run.sh
              envFrom:
                - secretRef:
                    name: backups-secrets
              env:
                - name: BACKUP_NAME
                  value: kitchenowl
                - name: DELETE_OLDER_THAN
                  value: "14d"
                - name: HEALTHCHECK_URL
                  value: "https://uptime.hemma.dev/api/push/uKjwRJNdHe?status=up&msg=OK&ping="
              volumeMounts:
                - name: backup-persistent-storage
                  mountPath: /kitchenowl
          restartPolicy: Never
          volumes:
            - name: backup-persistent-storage
              persistentVolumeClaim:
                claimName: kitchenowl
          affinity:
            podAffinity:
              requiredDuringSchedulingIgnoredDuringExecution:
                - labelSelector:
                    matchExpressions:
                      - key: app.kubernetes.io/name
                        operator: In
                        values:
                          - nextcloud
                  topologyKey: kubernetes.io/hostname
