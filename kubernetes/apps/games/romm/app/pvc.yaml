---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: romm-games
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: ""
  volumeMode: Filesystem
  volumeName: storage-zfs-games
