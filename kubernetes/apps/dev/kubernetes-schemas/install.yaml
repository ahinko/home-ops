---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: apps-kubernetes-schemas
  namespace: flux-system
  labels:
    substitution.flux.home.arpa/enabled: "true"
spec:
  path: "./kubernetes/apps/dev/kubernetes-schemas/app"
  sourceRef:
    kind: GitRepository
    name: homelab-k8s
  prune: true
  wait: true
  interval: 10m
