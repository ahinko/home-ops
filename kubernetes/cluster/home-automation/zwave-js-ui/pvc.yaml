---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: zwave-js-ui-config
  namespace: home-automation
  labels:
    backup.home.lab/enabled: "true"
    app.kubernetes.io/name: zwave-js-ui
    app.kubernetes.io/instance: zwave-js-ui
  annotations:
    backup.home.lab/exclude: ""
    backup.home.lab/healthcheck_url: "https://uptime.${SECRET_DOMAIN}/api/push/PEtksXrVie?status=up&msg=OK&ping="
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: rook-cephfs
