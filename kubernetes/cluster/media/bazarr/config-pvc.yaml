---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bazarr-config
  namespace: media
  labels:
    backup.home.lab/enabled: "true"
    app.kubernetes.io/name: &name bazarr
    app.kubernetes.io/instance: *name
  annotations:
    backup.home.lab/exclude: "./log"
    backup.home.lab/healthcheck_url: "https://uptime.${SECRET_DOMAIN}/api/push/SDMoLRG33c?status=up&msg=OK&ping="
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 4Gi
  storageClassName: rook-cephfs
