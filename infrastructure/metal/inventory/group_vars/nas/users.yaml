---
users:
  - name: peter
    shell: /bin/bash
    comment: The owner of the box
    password: "{{ user_password.peter }}"
    groups: backup,ahinko

  - name: ellen
    shell: /bin/bash
    comment: The one and only Ellen
    password: "{{ user_password_ellen }}"
    groups: backup,ahinko

user_groups:
  - name: backup
  - name: ahinko
