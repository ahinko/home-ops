---
apt_disable_recommends: "enabled"
apt_unattended_upgrades: "disabled"

apt_install_packages:
  - minicom # Update firmware on zwave stick
  - busybox-syslogd # read only fs

apt_remove_packages:
  - triggerhappy # read only fs
  - logrotate # read only fs
  - dphys-swapfile # read only fs
  - rsyslog # read only fs
