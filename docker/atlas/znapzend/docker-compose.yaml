---

version: '3.8'

services:
  znapzend:
    container_name: znapzend
    image: oetiker/znapzend:v0.21.1
    restart: always
    privileged: true
    devices:
      - "/dev/zfs"
    volumes:
      - /root/.ssh:/root/.ssh
