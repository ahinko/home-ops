---
version: "3"

dotenv: [".env"]

vars:
  PROJECT_ROOT:
    sh: git rev-parse --show-toplevel

tasks:
  default:
    silent: true
    cmds:
      - task --list

includes:
  tools: .taskfiles/tools.yaml
  management: .taskfiles/management.yaml
  metal: .taskfiles/metal.yaml
  flux: .taskfiles/flux.yaml
  maintenance: .taskfiles/maintenance.yaml
